{
  "name": "udf-viewer",
  "private": true,
  "version": "1.1.1",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "vite",
    "build": "vite build",
    "test": "node --test && cargo test --manifest-path src-tauri/Cargo.toml",
    "test:js": "node --test",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "test:parser": "node --test test/parser.test.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "jsdom": "^29.1.0",
    "vite": "^8.0.11"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "jszip": "^3.10.1"
  }
}
